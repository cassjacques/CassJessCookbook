<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <script src="/scripts/scripts.js" defer></script>
    <title>Edit Recipe</title>
</head>
<body>
    <%- include('../partials/recipeHeader') %>
    <main>

    <section>
        <h2>Edit <%= recipeData.recipeTitle %> Recipe Data</h2>
       <form action='/recipes/<%= recipeData._id %>?_method=PUT' method='POST'>

        <div>
            <label for='recipeTitle'>Title</label>
            <input type='text' id='recipeTitle' name='recipeTitle' value='<%= recipeData.recipeTitle %>' required>
        </div>

        <div>
            <label for='ingredients'>Ingredients</label>
            <input type='text' id='ingredients' name='ingredients' value='<%= recipeData.ingredients %>'>
        </div>

        <div>
            <label for='instructions'>Instructions</label>
           
            <input type='text' id='instructions' name='instructions' value='<%= recipeData.instructions %>'>
        </div>
    
        <div>
            <label for='recipeType'>Type</label>
            <input type='text' id='recipeType' name='recipeType' value='<%= recipeData.recipeType %>'>
        </div>

        <button type='submit'>Update</button>

        </form>
    </section>
    <br>
    <a href="/users/<%= recipeData.user._id %>">Go Back</a> 
    

    </main>

</body>
</html>

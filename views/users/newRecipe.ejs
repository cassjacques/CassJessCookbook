<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Recipe</title>
</head>
<body>
    
    <main>

        <h1>Add New Recipe</h1>

        <form action='/users/<%= userId %>/recipes' method='POST'>

            <div>
                <label for='recipeTitle'>Title</label>
                <input type='text' id='recipeTitle' name='recipeTitle' required>
            </div>

            <div>
                <label for='ingredients'>Ingredients</label>
                <input type='text' id='ingredients' name='ingredients' required>
            </div>

            <div>
                <label for='instructions'>Instructions</label>
               
                <textarea id='instructions' name='instructions' rows='5' columns='10'></textarea>
            </div>
        
            <div>
                <label for='recipeType'>Type</label>
                <input type='text' id='recipeType' name='recipeType'>
            </div>

            <button type='submit'>Add Recipe</button>

        </form>

        <a href="/users/<%= userId %>">Go Back</a>
    </main>
</body>
</html>
